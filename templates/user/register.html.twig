{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
{# Ajout de mt-5 pour la marge du haut #}
<div class="row justify-content-center align-items-center mt-5" style="min-height: 80vh;">
    <div class="col-md-8 col-lg-6">
        
        {# CARTE BLANCHE #}
        <div class="card-item p-4 p-md-5">
            
            <div class="text-center mb-4">
                <h2 class="fw-bold" style="color: var(--toon-blue);">Créer un compte 🚀</h2>
                <p class="text-muted small">Rejoignez-nous en quelques secondes.</p>
            </div>

            {{ form_start(registrationForm) }}
                
                {# 1. EMAIL (Vertical) #}
                <div class="mb-3 mt-7">
                    {{ form_row(registrationForm.email, {'attr': {'class': 'form-control', 'placeholder': 'exemple@email.com'}}) }}
                </div>
                
                {# 2. PRÉNOM et NOM (Horizontal : Côte à côte) #}
                <div class="row">
                    <div class="col-md-6 mb-3">
                        {{ form_row(registrationForm.firstName, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="col-md-6 mb-3">
                        {{ form_row(registrationForm.lastName, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                {# 3. TÉLÉPHONE (Vertical) #}
                <div class="mb-3">
                    {{ form_row(registrationForm.phoneNumber, {'attr': {'class': 'form-control'}}) }}
                </div>
                
                {# 4. MOT DE PASSE (Vertical) #}
                <div class="mb-4">
                    {{ form_row(registrationForm.plainPassword, {'attr': {'class': 'form-control'}}) }}
                </div>

                {# 5. BOUTON (Plus petit et centré) #}
                <div class="text-center mt-4">
                    <button type="submit" class="btn btn-primary px-5 py-2 fs-6 shadow-sm fw-bold rounded-pill">
                        M'INSCRIRE
                    </button>
                </div>

            {{ form_end(registrationForm) }}
            
            <div class="text-center mt-4 pt-3 border-top border-light">
                <p class="small text-muted mb-1">Vous avez déjà un compte ?</p>
                <a href="{{ path('app_login') }}" class="fw-bold text-decoration-none" style="color: var(--toon-orange);">
                    Se connecter
                </a>
            </div>
        </div>

    </div>
</div>
{% endblock %}